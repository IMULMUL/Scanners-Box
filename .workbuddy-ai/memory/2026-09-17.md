# 2026-09-17

## A³C 改名 + AI 提示框改版（已推送）

- 三份 README（EN/CN/ES）+ `Project-A3C.md` 的 A³C 标题同步改为 "Autonomous AI Agent Certification"：
  - EN: `Autonomous AI Agent Certification`
  - CN: `自主 AI Agent 认证计划`
  - ES: `Certificación de Agentes de IA Autónomos`
- 目录区 `[!IMPORTANT]` 提示框去掉 `❋⚛🐋 For AI`，改为 emoji 主标题 + 斜体副标题：
  `🤖 AI & Autonomous Agents` / `🤖 AI 与自主安全 Agent` / `🤖 IA y Agentes Autónomos`。
  注意：这段在 doctoc TOC 区块内，**不能用 `##` 标题**（会被 doctoc 收进 TOC 打乱结构），只能用 `**加粗**`。
- 顺手删掉 README.md 第 14 行多余的 `</p>`。
- `badges/A3C.png` 图内只有 "SCANNERS-BOX AUTHENTICATED"，无 Certification 字样 → 无需重新出图。
- commit `e03e8e4`，已 push 到 master。

## 仓库配置

- origin remote 原为 HTTPS，push 会卡在用户名密码输入 → 已改为 SSH：`git@github.com:We5ter/Scanners-Box.git`。
- 分支 master；本次连带推送了之前未推的 `456b6db`（ruoyi-scan）。

## A³C 体系现状盘点（诊断，未动手改）

- 全站 349 条目，**只有 5 个获证**（1.4%）；三语 README 各 5 个徽章，同步一致（CN 徽章文案是 "A³C 认证"，URL 编码 `%E8%AE%A4%E8%AF%81`，不是 `Certified`，grep 时别只搜英文）。
- 获证项目（全在 "AI Autonomous Cybersecurity Agents" 章节，6 条中 5 条）：
  `oritera/Cairn`、`KeygraphHQ/shannon`、`SickHackPark/SickHackShark`、`SanMuzZzZz/LuaN1aoAgent`、`Ed1s0nZ/CyberStrikeAI`
- 其余 5 个 AI 相关章节（LLM 扫描器 3 / AI 应用审计 4 / Agent 运行时 1 / Agent 技能审计 2 / 自主漏洞发现 2 = 12 条）**0 认证**。
- 统计口径：徽章是 shields.io 静态图，任何人都可复制伪造；认证名单（source of truth）不存在。
- 给用户的改进优先级：① 建 A3C-CERTIFIED.md 名单（含证书 ID / 认证日期 / 复审到期）② 量化认证标准 + 6 个月有效期与撤销条款 ③ 加免责声明（非代码审计、须授权使用）④ 名称收窄到 Agent，与正文 "AI-powered autonomous projects" 对齐。
- 未执行，等用户拍板。
